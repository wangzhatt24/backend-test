import Mailgen from 'mailgen';

const mailGenerator = new Mailgen({
  theme: 'salted',
  product: {
    name: 'batdongsan24.id.vn',
    link: 'https://batdongsan24.id.vn',
  },
});

export default mailGenerator;
